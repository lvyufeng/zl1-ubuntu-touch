# 170 — 第二个成因第一次上设备：判词是 `CONTAMINATED`，而"污染"的形状不是设计防的那个

**日期**: 2026-09-25
**状态**: **在设备上跑完了**（同一次开机 `2fbf9f8e`，和 docs 168 是同一次；没有重启、没有刷任何分区、
没有打开任何块设备、没有停容器、没有重启任何服务）。写的东西只有四个 cpufreq 文件，
写回去的值是修法自己的 `interactive`，跑完之后逐核读回确认。原始输出:
[`evidence/governor-temp-ab-2026-09-25.log`](evidence/governor-temp-ab-2026-09-25.log)。

**结果**: **`CONTAMINATED`（退出 1）——第二个成因的价钱这一趟没有量到**。
在 **B-A 最大的那个 zone**（`thermal_zone12` / `tsens_tz_sensor11`）上，
三个窗口是 A **40.9** → B **47.7** → C **48.0**：B 比 A 高 **6.8 °C**，
而**控制窗口 C（和 A 同一个状态）比 A 还高 8.4 °C**（最大值在 `thermal_zone9` / `tsens_tz_sensor8`）。
升温**没有跟着 governor 的状态回来**，所以这一趟不能归给 governor——
仪器自己这么说了，而**它是对的**。

**这一页同时记下这一趟暴露出的两个东西**：仪器里一条**不指名路径的"archive"消息，
底下还压着一条"第二个 EXIT trap 把第一个静默取消掉"的缺陷**（第 4 节），
以及这个三窗口设计**没有命名过的第二个盲区**（第 5 节）。

**接续**: [`169`](169-the-second-heat-cause-has-an-instrument.md)（这一台仪器的设计、两道只属于它的拒绝、
116 条离线检查）、[`166`](166-what-the-third-cause-is-worth-in-degrees.md) /
[`167`](167-the-third-cause-is-worth-5-5-degrees.md)（同一个设计量第三个成因，**成功过一次**）、
[`164`](164-the-third-heat-cause-is-installed-on-a-clean-baseline.md)（第二个成因装上的那一轮）。

---

## 1. 一句话结论

| 问题 | 答案 |
|---|---|
| 量到什么？ | **没有量到②的价钱。**判词 `CONTAMINATED`：B 比 A 高 6.8 °C，而**控制窗口 C 还比 A 高 8.4 °C**——升温没有跟着状态回来，所以它不能归给 governor。 |
| 拒绝全过了吗？ | 全过了，**而且是在真设备上过的**：panic 门（1 个 `download_mode` 参数读 0）、仪器可读、四个核可写**且两个值都被内核提供**、窗口 A 就是装好的状态，外加写路径的读回证明。 |
| 写下去了吗？写回来没有？ | 写下去了（`performance`，四个核都读回 `performance`），也**写回来了**（四个核读回 `interactive`）——跑完在设备上复核过，`boot_id` 没变、`systemctl --failed` 是 0。 |
| 那三个窗口的数字是什么？ | **B-A 最大的那个 zone**（`thermal_zone12` / `tsens_tz_sensor11`）逐窗口：**A 40.9 → B 47.7 → C 48.0**。busy：**A 0.81 / B 0.54 / C 0.82 of 4 cores**。**38 个 zone 的完整表**在存档第 E 节（它是从一个**泄漏在 `/tmp` 里**的目录里找回的）。 |
| 为什么 C 没回来？ | **因为这一趟的升温不是"在 B 里鼓起来、到 C 消失"的那种漂移，而是整趟单调上升。**三个窗口连起来看是一条上升线，负载没变（busy 在 B 那趟**最低**），所以它读起来像"干预把 SoC 加热了，而 45 秒的修法状态不够把 7 °C 的热质量散掉"。**38 行里有 25 行的 C-A 是正的**，而电池、eMMC、pa_therm 和 pm8994 几乎没动——所以这是 SoC，不是整机被外面烘热。 |
| 这对设计意味着什么？ | 控制窗口只能抓住**在 C 之前消失**的漂移。一个**在窗口里根本不衰减**的升温，它分不出"这个成因值 6.8 °C"和"干预加热了芯片、而窗口没等到它凉下来"。**这是设计没有命名过的第二个盲区**（第 5 节）。 |
| 还坏了什么？ | 仪器的一句话：`... 26 more; the whole table is in the archive`——**"archive" 指的是 `$TMP/deltas`，而这条路径从来没被印出来**。查下去发现底下还有一层：脚本第 90 行有 `trap 'rm -rf "$TMP"' EXIT`，第 184 行又有 `trap 'do_restore' EXIT`，**POSIX 里第二个 EXIT trap 会替换掉第一个**——所以清理**从来没跑过**（每次调用都泄漏一个目录，连只读的 `--status` 也泄漏），而那句话又不指名路径（第 4 节）。 |
| 设备现在怎么样？ | `boot_id` 未变，四个核 `interactive`，`systemctl is-system-running` = `running`，`--failed` = 0 个单元。**它从来没有处在任何危险状态里。** |

---

## 2. 这一趟跑的是什么，以及它为什么值得跑

docs 169 写完的仪器**一次都没在设备上跑过**——它的判词、退出码、trap、读回、四道拒绝，
全部是对着一个假设备验证的。这一趟买的是**第一件只有真设备能回答的事**：
**那四道拒绝在这台机器上过不过**，以及**那个写到底会不会落地**。

过。而且不是"没报错"式的过：

```
   A. panic guard: all 1 download_mode parameter(s) read 0 -- a panic reboots, not EDL
   B. instrument: readable (/tmp/zl1-thermal.sh)
   C. cores:      all 4 are writable and offer both 'interactive' and 'performance'
   D. as installed: all 4 core(s) read 'interactive' -- the fix is in place (this is window A)
   C. write path: wrote 'interactive' back to all 4 core(s) and read 'interactive'
                  back -- writable in fact, and every core agrees on what it read.
```

拒绝 C 的最后一条**是设备自己说的**：内核确实提供 `interactive` 和 `performance` 两个值
（这一条在 docs 169 里是离线读的 `scaling_available_governors`，这里是在**真的写下去之前**又证明了一遍）。
窗口 A 也确实是装好的状态——**如果四个核里有任何一个不是 `interactive`，这一趟在写第一个字节之前就会退出**。

而这一次干预**是两向都读回过的**：写 `performance` 之后四个核读回 `performance`，
写 `interactive` 之后四个核读回 `interactive`。跑完在设备上复核：`boot_id` 没变，四个核是 `interactive`。
**仪器把它借走的东西还回来了。**

---

## 3. 判词是 `CONTAMINATED`，而这是仪器的正确行为

```
   the largest warming from A to B on any tsens zone: 6.8 C  (thermal_zone12 (tsens_tz_sensor11))
   and from A to C (the control, same state as A):   8.4 C  (thermal_zone9 (tsens_tz_sensor8))

   -> CONTAMINATED: B was warmer than A by 6.8 C, but C -- the SAME state as A -- is still 8.4 C
      above A. So the warming did not come back with the governor's state and was not caused by it:
      the phone drifted, or the load did. A statement about this RUN, not about the governor.
```

**先说清楚这三行印的是什么**：它们不是"最热的那一个 zone"的温度，而是**B-A 最大的那个 zone**。
上面第一行里的三个数（40.9 / 47.7 / 48.0）属于**同一个** zone（`thermal_zone12`），
而 A 这一列里**最热**的 tsens zone 是 `thermal_zone18`（42.7 °C）——两者不是同一个。
这个区别在只印 12 行的时候看不出来，是完整表（存档第 E 节）把它显出来的。

如果这台仪器只有 A 和 B 两个窗口，它现在会**印出 6.8 °C 并把它当作 governor 的价钱**。
控制窗口的全部意义就是让这件事**不能**发生，而它这一趟做到了：**它报出了一条对手机什么都没说的结论**。
这正是 docs 166 把第三个窗口写进设计里的理由，也是 docs 167 那次为什么那条读数是可信的
（那一次 C 回到了 0.0 °C、而且是**反方向**的 0.7–1.3 °C）。

---

## 4. 缺陷一：一句不指名路径的 "archive"，底下压着一个**被静默替换掉的 EXIT trap**

38 个 zone 读到了，**只印出 12 行**，然后是一句：

```
   ... 26 more; the whole table is in the archive (it is one line per zone)
```

这句话**没有路径**。而"archive"指的是 `$TMP/deltas`，`$TMP` 是一个带随机后缀的目录
（`/tmp/zl1-governor-temp-ab.XXXXXX`）。同一句话在 `run_window()` 里也有一个变体
（"its output is in the archive"）。

**这一页的第一版把这件事写错了，而错的是"东西被删掉了"那半句——而且那一版已经提交、已经推送**
（`7e971a4`）。错在**读了源码就下结论**：看到第 90 行的 `trap 'rm -rf "$TMP"' EXIT`，
就以为目录被删了。真正把这件事定下来的是**上设备看一眼**：

```
$ ls -d /tmp/zl1-governor-temp-ab.*
/tmp/zl1-governor-temp-ab.HZcZCl        <- 空的：只读的那次 --status 泄漏的
/tmp/zl1-governor-temp-ab.MlHTie        <- 这一趟：deltas, deltas.note, win.A, win.B, win.C
/tmp/zl1-governor-temp-ab.sh
```

脚本里**确实**有 `trap 'rm -rf "$TMP"' EXIT`——在第 90 行，拒绝之前。而第 184 行还有
`trap 'do_restore' EXIT`。**POSIX 里第二个 EXIT trap 会替换掉第一个**，所以那段清理
**从来没有执行过一次**：每一次调用都泄漏它的草稿目录，**包括只读的 `--status`**（上面那个空目录就是它）。
所以那条读数**不是**被删掉了——真正的缺陷是两条合起来：
**(i) 没有任何东西被清理，而且是静默的；(ii) 那句话指向它却不指名它。** 对一个操作者来说，
这两条是同一件事：**证据在，但你找不到它**。

这是"两个 trap、一个 EXIT、第二个静默取消第一个"的形状，而且它**从通过的那一侧看不见**：
所有关于"恢复有没有生效"的断言都还成立，因为没有一条断言问过"那个目录还在不在"。
完整的那 38 行表就是从那个泄漏的目录里**找回来的**（存档第 E 节）。

修法（还没做）：**一个** trap 同时管恢复和清理（这样它们不可能互相取消），
`--keep` 时**不删并且把路径印出来**，而且那条"还有 N 行"的消息里**必须带路径**——
或者干脆把整张表印出来（38 行在一个手机终端上完全可以承受，而这一页正好说明"省掉 26 行"
换来的是一条**没人能找到**的证据）。

---

## 5. 缺陷二：这个设计防的是"在 C 之前消失的漂移"，而这一趟的升温在窗口里根本不衰减

三个窗口在 **B-A 最大的那个 zone**（`thermal_zone12`）上的读数：

```
   A  40.9      B  47.7      C  48.0
```

三个窗口的 busy：

```
   A  0.81      B  0.54      C  0.82     (of 4 cores)
```

**负载没有变，而且 B（被钉住的窗口）的 busy 反而是三个里最低的**——
这本身就是仪器自己写明的理由：governor 改的是核跑的**频率**，而 busy 数的是**时间**，
所以这两个量**本来就不该同步**。于是"这一趟的升温是负载造成的"这个解释被这条读数关掉了。

剩下的解释里，最贴合形状的是**热质量**：干预把 SoC 加热了（B 比 A 高 6.8 °C），
而把四个核放回 `interactive` 之后，**45 秒不够把 7 °C 散掉**，所以 C 读到的仍是热的那台机器
（48.0 °C，甚至比 B 还高一点）。

**这就是设计的第二个盲区，而它没有被命名过。** docs 166/169 写下的那个盲区是
"一个正好在窗口 B 里冲高、到 C 消失的漂移，和这个效应在这套设计里无法区分"——
但这一趟发生的**不是**那件事：这里的升温**根本没有消失**，它整趟都在。
两者的后果不同：前者会**虚报**一个价钱，后者会让**判词永远停在 `CONTAMINATED`**，
即**一个只会说"我不知道"的仪器**。

**修法不是"把 settle 调大"**——那是同一个猜测往上抬一档，而且它只是把"多长才够"这个问题
从 45 秒挪到 120 秒。可判定的版本是：**在开 C 之前，等读数回到 A 的一个具名容差之内，并给这个等待设一个上限**
（`--settle-back N`，容差和上限都印出来），然后**把等了多少秒说出来**。
如果在上限里**没有**回来，那本身是一条**关于这台手机的读数**——
"这个干预的升温在 N 秒内不衰减"——而它**仍然不是**"governor 值 6.8 °C"。
这两种结果必须走不同的退出码，否则仪器又会把"我不知道"和"价钱是这么多"混在一起。

热时间常数应该先**量一次**再定上限：这一趟的数据够估一个下界（45 秒内没有可见衰减），
但真正的常数要一个专门的小实验（放回 `interactive` 之后每 5 秒读一次，看它多久回到 A）。

---

## 6. 这一页**没有**成立的东西

* **②的价钱还是没有。**这一趟没有量到它，也没有反驳它。说"governor 让这台机器热 6.8 °C"是**错的**，
  说"governor 对温度没有影响"**同样没有依据**——这一趟两者都没有测。
* **一趟、一个负载、一个环境。**环境温度、充电状态、屏上有什么，**都不受控**，
  这和 docs 167 的 5.5 °C 带的那组限定是同一条。
* **热时间常数没有量过。**上面那个"45 秒不够散掉 7 °C"是**对形状的解释**，不是一条读数——
  它要一个自己的实验（第 5 节最后一段）。
* **跨重启的持久性仍然没量过**（docs 168 就留着这一格）：这一趟没有重启。
* **设备时钟是错的**：它读 1970-02-13，所以仪器印出来的每一个时间戳都是错的，
  真实日期在存档的表头里（同一个缺陷 docs 100 记过）。这一条**不影响任何温度读数**，
  但它意味着**任何按时间戳对齐这台设备的记录都会错**。

---

## 7. 阶段位置

散热那三条：①和③都有读数，**②现在有过一次真机运行，而那次运行说"我不知道"**——
这比一个错的数字好，但它还不是价钱。这一趟买到的东西按重要性排：

| | 这一趟之前 | 这一趟之后 |
|---|---|---|
| 四道拒绝在真机上 | 只有离线 | **全过，而且是写之前的** |
| 写路径 | 只有离线 | **真机上两向都读回证明过** |
| ②的价钱 | 没有 | **仍然没有**（`CONTAMINATED`，这是诚实的结果） |
| 仪器的这句话 | "整张表在 archive 里" | **已知是错的**：它不指名路径，而且清理 trap 被静默取消了（第 4 节）；那 26 行是从 `/tmp` 里泄漏的目录找回来的 |
| 设计 | 一个已命名的盲区 | **第二个盲区被这次运行指出**：升温不衰减时，控制窗口只会一直说不知道 |

下一步是**把这两个缺陷都修掉**（第 4、5 节）——**两件都在 [`171`](171-the-second-cause-had-two-defects-and-both-are-fixed.md) 里做完了**
（整张表印出来、运行说出草稿目录在哪、一个 EXIT trap 同时管恢复和清理；以及在窗口 C 之前**等读数回来**，
把"没回来"变成判词 `no-return`），其中第二个的修法要先量一次**热时间常数**——
而这件事**下一次上设备时才会量到**（那个"等了多少秒"就是它）。这两件事**全部离线可做**，做完再上设备。
硬件清单上还没驱动起来的那几项
（相机 app 上没上屏、GPS 一次定位都没有、指纹的驱动编进了镜像但没刷）这一轮同样**一个都没有碰**。
